<source>
  type forward

  format syslog
  port 5514
  bind 0.0.0.0
</source>

<filter **>
  @type parser

  format syslog
  key_name Message
  reserve_data true
</filter>

<match **>
    type stdout
</match>