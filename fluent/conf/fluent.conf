<source>
  @type syslog
  port 5514
  bind 0.0.0.0
  tag pfsense

  format grok
  grok_pattern %{SYSLOGTIMESTAMP:date} %{WORD:prog}: %{LOG_DATA}%{IP_SPECIFIC_DATA}%{IP_DATA}
  custom_pattern_path /fluentd/etc/patterns
</source>

<match pfsense.**>
  type stdout
</match>