elasticsearch:
  image: airforon/elasticsearch
  container_name: elasticsearch
  ports:
    - 9200:9200
  environment:
    CLUSTER_NAME: "es"
    NODE_NAME: "node0"
    NODE_RACK: "es1"
fluent:
  build: fluent
  container_name: fluent
  environment:
    TIMEZONE: "Asia/Tokyo"
    HOSTNAME: "air-foron.com"
  ports:
    - "5514:5514/udp"
  links:
    - elasticsearch
kibana:
  image: airforon/kibana
  container_name: kibana
  ports:
    - 9204:5601
  environment:
      - ELASTICSEARCH_URL=http://elasticsearch:9200
  links:
      - elasticsearch